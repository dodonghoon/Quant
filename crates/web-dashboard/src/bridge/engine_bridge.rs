//! # Strategy Engine Bridge
//!
//! Strategy Engine ↔ 웹 API 연결.
//! 모델 상태와 시그널을 실시간으로 웹에 전달합니다.

/// Strategy Engine → Dashboard 브릿지 (향후 구현)
pub struct StrategyBridge;

impl StrategyBridge {
    pub fn new_demo() -> Self {
        Self
    }
}
