//! # Execution Engine Bridge
//!
//! Execution & Risk Engine ↔ 웹 API 연결.
//! Kill Switch, OMS, Risk 상태를 웹에 전달합니다.

/// Execution Engine → Dashboard 브릿지 (향후 구현)
pub struct ExecBridge;

impl ExecBridge {
    pub fn new_demo() -> Self {
        Self
    }
}
